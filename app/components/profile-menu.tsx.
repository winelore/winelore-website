"use client"

import Image from "next/image"
import { User, LogOut, Wine, Trophy, ClipboardList, ExternalLink } from "lucide-react"
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"
import { Separator } from "@/components/ui/separator"

export function ProfileMenu() {
  return (
    <Popover>
      <PopoverTrigger asChild>
        <button className="flex items-center outline-none group">
          <div className="h-10 w-10 rounded-full border-2 border-indigo-400 flex items-center justify-center transition-all group-hover:border-indigo-500 group-active:scale-95">
            <User className="h-6 w-6 text-indigo-400" />
          </div>
        </button>
      </PopoverTrigger>

      <PopoverContent
        align="end"
        sideOffset={8}
        className="w-64 rounded-3xl p-0 shadow-lg border border-gray-100 overflow-hidden bg-white"
      >
        {/* Кнопки Profile та Log Out */}
        <div className="flex items-center justify-between p-3 gap-2">
          <button className="flex flex-col items-center gap-1 flex-1 py-2 rounded-xl bg-gray-100 hover:bg-gray-200 transition-colors group">
            <User className="h-5 w-5 text-gray-700" />
            <span className="text-xs font-semibold text-gray-700">Profile</span>
          </button>
          <button className="flex flex-col items-center gap-1 flex-1 py-2 rounded-xl hover:bg-red-50 transition-colors group">
            <LogOut className="h-5 w-5 text-red-500" />
            <span className="text-xs font-semibold text-red-500">Log Out</span>
          </button>
        </div>

        {/* Основне меню */}
        <div className="px-2 pb-2">
          <button className="flex items-center gap-3 w-full px-3 py-2.5 rounded-xl hover:bg-gray-50 transition-colors text-left">
            <Wine className="h-5 w-5 text-gray-500" />
            <span className="text-sm font-medium text-gray-900">My Beverages</span>
          </button>
          <button className="flex items-center gap-3 w-full px-3 py-2.5 rounded-xl hover:bg-gray-50 transition-colors text-left">
            <Trophy className="h-5 w-5 text-gray-500" />
            <span className="text-sm font-medium text-gray-900">My Competitions</span>
          </button>
        </div>

        <div className="px-4">
          <Separator className="bg-gray-100" />
        </div>

        <div className="px-2 py-2">
          <button className="flex items-center gap-3 w-full px-3 py-2.5 rounded-xl hover:bg-gray-50 transition-colors text-left">
            <ClipboardList className="h-5 w-5 text-gray-500" />
            <span className="text-sm font-medium text-gray-900">My Assessments</span>
          </button>
        </div>

        {/* Секція AXUS ID */}
        <div className="px-2 pb-2">
          <div className="mx-2 mb-2 border-t border-gray-100" />
          <button className="w-full flex items-center justify-between px-3 py-3 hover:bg-gray-50 rounded-xl transition-colors group text-left">
            <div className="flex items-center gap-3">
              <Image
                src="/axus-logo.png"
                alt="AXUS ID Logo"
                width={20}
                height={20}
                className="object-contain"
              />
              <span className="text-sm font-bold text-gray-800">AXUS ID Profile</span>
            </div>
            <ExternalLink className="h-4 w-4 text-gray-400 group-hover:text-gray-600 transition-colors" />
          </button>
        </div>
      </PopoverContent>
    </Popover>/vercel/share/v0-project/public/axus-logo.png
  )
}