import { TemplateEditorSheet } from "../../src/templates/TemplateEditorSheet"

/** A new template, in the web's editor as a page sheet. */
export default function NewTemplateRoute() {
    return <TemplateEditorSheet />
}
