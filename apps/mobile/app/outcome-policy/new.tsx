import { PolicyEditorSheet } from "../../src/outcomePolicies/PolicyEditorSheet"

/** Creating an outcome policy — the web's create dialog, at the path of its create page. */
export default function NewOutcomePolicyRoute() {
    return <PolicyEditorSheet />
}
