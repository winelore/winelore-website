export { TabStack as default } from "../../../src/navigation/TabStack"

/** The tab opens on its list; a deep link to anything inside it still has that to go back to. */
export const unstable_settings = { anchor: "competitions" }
